on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build binaries
        run: |
          GOOS=linux GOARCH=amd64 go build -mod vendor -o gce-deploy-action.linux.amd64 . 
          GOOS=darwin GOARCH=amd64 go build -mod vendor -o gce-deploy-action.darwin.amd64 . 

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload linux.amd64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} 
          asset_path: gce-deploy-action.linux.amd64
          asset_name: gce-deploy-action.linux.amd64
          asset_content_type: application/octet-stream

      - name: Upload darwin.amd64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} 
          asset_path: gce-deploy-action.darwin.amd64
          asset_name: gce-deploy-action.darwin.amd64
          asset_content_type: application/octet-stream
