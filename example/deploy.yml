deploys:
  - name: my-deploy
    region: us-central1
    instance_group: my-instance-group
    instance_template_base: instance-template-base
    instance_template: instance-template-$GITHUB_RUN_NUMBER-$GITHUB_SHA
    startup_script: cloud-config.yml
    script_vars:
      my_script_var: "The current directory is:"
